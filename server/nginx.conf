events {}

http {
    server {
        access_log /var/log/nginx/access.log combined;

        listen 80;
        listen [::]:80;

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}
